# GroveDB C++ Rewrite: Rust ↔ C++ Feature Matrix

This document describes the feature parity between Rust GroveDB v4.0.0 and the C++ rewrite.
Last reviewed: 2026-03-04

Legend:
- Full: implemented with parity intent and covered by tests
- Divergent: implemented but not aligned with Rust behavior (see accepted-divergences.md)

| Parity | Target | Area | Rust (module / API) | C++ (header / class) | Notes |
| --- | --- | --- | --- | --- | --- |
| Full | Full | Core DB open/close | `grovedb::GroveDb::open`, `wipe` | `GroveDb::Open`, `GroveDb::Wipe` (`include/grovedb.h`) | Facade implements complete API surface: `Open`, `Wipe`, `Flush`, checkpoint ops (`CreateCheckpoint`, `DeleteCheckpoint`, `OpenCheckpoint`), basic CRUD (`Insert`, `Delete`, `Get`), typed insert helpers (`InsertItem`, `InsertEmptyTree`, `InsertBigSumTree`, `InsertCountTree`, `InsertProvableCountTree`, `InsertProvableCountSumTree`, `InsertSumItem`, `InsertItemWithSum`, `InsertReference`), conditional inserts (`InsertIfNotExists`, `InsertIfNotExistsReturnExisting`, `InsertIfChangedValue`), subtree ops (`ClearSubtree`, `DeleteSubtree`, `DeleteIfEmptyTree`, `DeleteUpTreeWhileEmpty`), reference resolution (`FollowReference`), raw element access (`GetRaw`, `GetRawOptional`, `GetCachingOptional`, `GetRawCachingOptional`), key existence checks (`HasRaw`, `HasAux`, `HasCachingOptional`), query APIs (`QueryRaw`, `QueryRawKeysOptional`, `QueryKeysOptional`, `QueryItemValue`, `QueryItemValueOrSum`, `QuerySums`, `QueryKeyElementPairs`), auxiliary storage (`PutAux`, `GetAux`, `DeleteAux`), root metadata (`RootKey`, `RootHash`), subtree discovery (`FindSubtrees`, `FindSubtreesByKinds`, `CountSubtrees`, `GetSubtreeRoot`, `CheckSubtreeExistsInvalidPath`), verification (`VerifyGroveDb`), batch ops (`ApplyBatch`, `ValidateBatch`, `EstimatedCaseOperationsForBatch`), version-gated wrappers (`*ForVersion`), and transaction overloads for all major APIs. All APIs covered by contract tests and Rust-backed parity modes in `rust_grovedb_facade_parity_tests`. |
| Full | Full | Transactions | `Transaction`, transactional Merk ops | `RocksDbWrapper::Transaction` + `GroveDb::Transaction` | Storage-layer transaction parity is complete (read-your-writes, snapshot isolation, conflict matrix including disjoint/prefix cases, rollback, iterator/scan, API misuse contracts). Rust-backed GroveDb facade parity reflects observed facade behavior: uncommitted insert visibility in-tx only, read-committed visibility for pre-opened concurrent tx (`tx_read_committed_visibility`, `tx_has_read_committed_visibility`, `tx_query_range_committed_visibility`, `tx_iterator_stability_under_commit`, and `tx_read_only_then_writer_commit`), with explicit C++ `GroveDb::Has` API coverage for the `has_raw` parity path, idempotent rollback-after-rollback on the same handle (`tx_double_rollback_noop`), persisted-state parity after conflict-heavy lifecycle sequences (`tx_conflict_sequence_persistence`), and broad late-commit conflicts (including disjoint key/path reverse and forward order conflict modes). Additional covered modes include mixed same-subtree tx mutations (`tx_mixed_commit`), read semantics after rollback (`tx_rollback`, `tx_rollback_range`), commit-after-rollback acceptance (`tx_commit_after_rollback_rejected`), write-after-rollback acceptance (`tx_write_after_rollback_rejected`), repeated rollback/write reuse (`tx_multi_rollback_reuse`), delete-after-rollback (`tx_delete_after_rollback`), tx delete visibility (`tx_delete_visibility`), same-key mutation ordering in one tx (`tx_delete_then_reinsert_same_key`, `tx_insert_then_delete_same_key`), missing-key delete no-op (`tx_delete_missing_noop`), delete-vs-insert ordering matrix across same key/same subtree disjoint/disjoint subtree (`tx_delete_insert_*` modes), replace-vs-delete and replace-vs-replace ordering matrix (`tx_replace_*` modes), same-key reverse-order conflict (`tx_same_key_conflict_reverse_order`), shared-subtree disjoint-key reverse-order conflict (`tx_shared_subtree_disjoint_conflict_reverse_order`), disjoint-subtree reverse-order conflict (`tx_disjoint_subtree_conflict_reverse_order`), disjoint-subtree forward-order conflict (`tx_disjoint_subtree_conflict_forward_order`), rollback-reopen visibility (`tx_reopen_visibility_after_rollback`), rollback-reopen same-path conflict (`tx_reopen_conflict_same_path`), and abort-on-drop rollback (`tx_drop_abort`) in `rust_grovedb_facade_parity_tests`; C++ contract tests also cover commit-after-commit/rollback-after-commit lifecycle errors and `QueryRaw` tx lifecycle checks (inactive/committed rejection + active-tx visibility), `QueryRawKeysOptional` tx lifecycle checks (inactive/committed rejection + tx-local visibility), `QueryKeysOptional` tx lifecycle checks (inactive/committed rejection + tx-local visibility), and Rust-backed parity modes `facade_query_raw_tx`, `facade_query_raw_keys_optional_tx`, `facade_query_keys_optional_tx`, `facade_insert_if_not_exists_return_existing_tx`, `facade_query_item_value_tx`, `facade_follow_reference_tx`, `facade_get_caching_optional_tx`, `facade_get_subtree_root_tx`, `facade_has_caching_optional_tx`, `facade_query_item_value_or_sum_tx`, `facade_check_subtree_exists_invalid_path_tx`, and `facade_query_key_element_pairs_tx` in `grovedb_facade_api_contract_tests` + `rust_grovedb_facade_parity_tests`, and shared-parent disjoint + same-path conflict cases are additionally covered in `rust_grovedb_tx_conflict_parity_tests`. All major facade APIs have tx overloads with lifecycle/visibility parity coverage. |
| Full | Full | Batch operations | `grovedb::batch` (validate/apply, atomic) | `GroveDb::{ApplyBatch, ApplyPartialBatch, ContinuePartialApplyBatch, ValidateBatch, EstimatedCaseOperationsForBatch}` + `RocksDbWrapper::WriteBatch` | Storage batch parity is implemented. GroveDb facade exposes `ApplyBatch` with Rust-style transactional execution: atomic local batch (`auto-begin + commit/rollback`) and external-tx batch execution semantics, plus `ValidateBatch` dry-run semantics (apply in tx + rollback). Covered in `grovedb_facade_tests` and `grovedb_facade_api_contract_tests`. C++ implements batch ops: `kInsert`, `kReplace`, `kDelete`, `kInsertOnly`, `kDeleteTree`, `kPatch`, `kRefreshReference`, `kInsertOrReplace`, `kInsertTree`. Rust-backed fixture parity modes: `batch_apply_local_atomic`, `batch_apply_tx_visibility`, `batch_apply_empty_noop`, `batch_validate_success_noop`, `batch_validate_failure_noop`, `batch_insert_only_semantics`, `batch_replace_semantics`, `batch_validate_no_override_insert`, `batch_validate_no_override_insert_only`, `batch_validate_no_override_replace`, `batch_validate_no_override_tree_insert`, `batch_validate_strict_noop`, `batch_delete_non_empty_tree_error`, `batch_delete_non_empty_tree_no_error`, `batch_disable_consistency_check`, `batch_disable_consistency_last_op_wins`, `batch_disable_consistency_reorder_parent_child`, `batch_insert_tree_semantics`, `batch_apply_failure_atomic_noop`, `batch_delete_missing_noop`, `batch_apply_tx_failure_atomic_noop`, `batch_apply_tx_failure_then_reuse`, `batch_apply_tx_failure_then_rollback`, `batch_apply_tx_success_then_rollback`, `batch_apply_tx_delete_then_rollback`, `batch_apply_tx_delete_missing_noop`, `batch_delete_tree_op`, `batch_delete_tree_disable_consistency_check`, `batch_delete_tree_non_empty_options`, `batch_mixed_non_minimal_ops`, `batch_mixed_non_minimal_ops_with_options`, `batch_refresh_reference_trust`, `batch_insert_tree_below_deleted_path_consistency`, `batch_insert_or_replace_semantics`, `batch_patch_existing`, `batch_patch_missing`, `batch_patch_strict_no_override`, `batch_insert_tree_with_root_hash`, `batch_validate_no_override_tree_insert_or_replace`. C++ `kDeleteTree` enforces tree-only key validation and mirrors Rust by deleting tree keys without consulting `allow_deleting_non_empty_trees` / `deleting_non_empty_trees_returns_error`. `kPatch` follows Rust batch patch flow as insert-or-replace with strict no-override option handling. Consistency prechecks mirror Rust scope for delete-subtree conflicts (flagging only `kReplace`/`kInsertOrReplace`), runtime enforces Rust-aligned insert-under-deleted-subtree rejection for insert-like subtree mutations. `validate_insertion_does_not_override_tree` enforced with Rust-aligned semantics. `trust_refresh_reference` enforced for `kRefreshReference`. Non-minimal option fields include pass-through coverage for `base_root_storage_is_free` and partial batch pause/resume support via `ApplyPartialBatch` and `ContinuePartialApplyBatch`; `ApplyBatch` deterministically requires the partial API when `batch_pause_height > 0`. `EstimatedCaseOperationsForBatch` (with options overload) provides no-write batch cost precomputation. All batch ops covered by Rust-backed parity in `rust_grovedb_facade_parity_tests`. |
| Full | Full | Merk tree core | `merk::Merk` (persistent, lazy links) | `MerkTree` (in-memory AVL) + `MerkStorage` | C++ MerkTree is in-memory with cached node hashes and link metadata for hash/count/sum/height. Lazy on-demand loading is fully covered for traversal, proof paths (present/range/absence/chunk), and metadata queries (`GetNodeMeta` uses lazy `EnsureChildLoaded` traversal instead of `EnsureFullyLoaded`). `MerkStorage::SaveTree` attaches storage context after commit via `AttachStorage`, enabling lazy-load operations without separate `LoadTree` round-trip, matching Rust's post-commit merk behavior. `SaveTreeToBatch` attaches transactional storage context after save for post-save lazy-link traversal semantics. Tx-attached lazy traversal falls back to base storage after tx commit so the same merk instance remains usable post-commit. `MerkTree::LoadEncodedTree(storage, path, root_key)` loads from `kDefault` (Rust-aligned base merk reopen). Empty-save lifecycle finalization is aligned across `SaveTree` and `SaveTreeToBatch` (both consume dirty/deleted trackers and keep storage attached). Clear-and-reopen lifecycle parity is covered (empty-root-hash parity after clear, disjoint-key reinsertion with old-key absence). Rust-backed persistent lifecycle parity includes explicit reopen flows: `merk_lifecycle` (root), `merk_child_lifecycle` (nested subtree), `merk_tx_lifecycle` (transaction-scoped reopen-and-mutate), `merk_incremental_save` (multi-phase incremental commit), and `cross_tx_lazy_load_after_commit` (post-tx-commit lazy-link fallback). Feature-variant persisted-format parity passes for all variants (basic/sum/big_sum/count/count_sum/prov_count/prov_count_sum) via `merk_persisted_format_report_*` and `rust_merk_feature_storage_parity_tests`. |
| Full | Full | Merk open/commit | `Merk::open_*`, `commit`, `clear` | `MerkTree` + `MerkStorage` | Incremental commit syncs dirty/deleted keys and roots with feature/aggregate tag encoding. Empty-tree saves mark persisted-root state for correct clear-then-reinsert semantics. Batch save stages subtree clears as prefix-delete batch ops (`WriteBatch::DeletePrefix`). Empty-save lifecycle finalization aligned across `SaveTree`/`SaveTreeToBatch` (both consume dirty/deleted trackers, keep storage attached). `SaveTreeToBatch` attaches transactional storage context for post-save lazy-link traversal; tx-attached lazy traversal falls back to base storage after tx close. `MerkTree::LoadEncodedTree` loads from `kDefault` (Rust-aligned base merk reopen). Clear-and-reopen lifecycle parity covered (empty-root-hash parity after clear, disjoint-key reinsertion `k10`/`k20` with old-key absence). Rust-backed persistent lifecycle parity includes explicit reopen flows: `merk_lifecycle` (root), `merk_child_lifecycle` (nested subtree), `merk_tx_lifecycle` (transaction-scoped reopen-and-mutate), `merk_incremental_save` (multi-phase incremental commit), `cross_tx_lazy_load_after_commit` (post-tx-commit lazy-link fallback). Feature-variant persisted-format parity passes for all variants (basic/sum/big_sum/count/count_sum/prov_count/prov_count_sum) via `merk_persisted_format_report_*` and `rust_merk_feature_storage_parity_tests`. Child subtree clear lifecycle covered by `merk_child_clear_lifecycle`. |
| Full | Full | Merk proofs | `merk::prove`, `proofs::encoding` | `MerkTree::GenerateProof*`, `proof.cpp` | C++ supports common ops, counted proofs, and encoding; Rust parity includes execution/negative/rewrite checks for single-key/absence/range and full chunk-set (`1..len`) parity with execution/truncation/mutation checks. Rust-backed merk proof parity now also covers persisted-link boundary range fixtures for right-edge absence (`k4..=k5`), left-edge boundary inclusion (`k0..=k1`), exclusive-end range (`k1..k3`), leftmost-key present (`k1`), and rightmost-key present (`k3`) against Rust bytes (`rust_merk_proof_parity_tests` + `rust_merk_proof_writer`; 11 fixtures total). Opcode coverage now explicitly validates decode support for all Rust `proofs::encoding` variants (`0x01..0x1b` and control ops `0x10..0x13`), and `CollectProofKeys` now correctly omits `KVHash` opcodes (`0x02`/`0x09`) from key extraction to match Rust decoding semantics. |
| Full | Full | Merk query proofs | `merk::proofs::query` | `query.h` | GroveDB-level query-proof pipeline is fully reintroduced with comprehensive query-shape coverage. `grovedb_facade_proof_tests` includes non-root prove+verify integration for all range types: `RangeAfter` (P-015, P-058), `RangeToInclusive` (P-030, P-081), `RangeTo` (P-036, P-159), `RangeFrom` (P-058), `RangeAfterToInclusive` (P-081, P-091), `RangeAfterTo` (P-096), `RangeInclusive` (P-118), `RangeFull` (P-131). Absence proof coverage for empty-result ranges: `RangeAfter` with keys beyond last key (P-154), `RangeTo` with keys before first key (P-159). Unbounded-upper range fix ensures `RangeFull`/`RangeFrom`/`RangeAfter` include the rightmost key (P-030). All query shapes covered with `ProveQueryForVersion` + `VerifyPathQueryProofForVersion` integration tests.
| Full (core) | Full | Chunk proofs | `merk::proofs::chunk`, `calculate_chunk_depths` | `MerkTree::GenerateChunkProof*`, `chunk` | Chunk ops/queries implemented with parity tests. |
| Full (core) | Full | Chunk producer | `merk::chunks::ChunkProducer` | `ChunkProducer` | C++ mirrors chunk producer behavior with tests. |
| Full (core) | Full | Chunk restore | `merk::restore::Restorer` | `ChunkRestorer` | C++ restores encoded nodes and validates metadata; still in-memory tree. |
| Full | Full | Grove proofs | `grovedb::operations::proof` | `proof.h`, `proof.cpp`, `grovedb.h` (`GroveDb::ProveQueryForVersion`) | GroveDB-level proof pipeline is fully implemented behind version gates. C++ facade exposes version-gated `GroveDb::ProveQueryForVersion` for root and non-root path queries with layered subquery generation, with generated-proof verification coverage in `grovedb_facade_proof_tests` and contract checks in `grovedb_facade_api_contract_tests`. Rust-backed parity includes dedicated `ProveQueryForVersion` generation fixtures (`rust_grovedb_proof_writer` + `rust_grovedb_proof_parity_tests`) covering both root single-key and layered subquery-path generation scenarios with version-matrix verification checks (`4.0.0`, `4.1.0`, `5.0.0`). End-to-end verify parity for full Grove proof execution is covered across supported versions (`4.0.0`, `4.1.0`, `5.0.0`) in `rust_grovedb_proof_parity_tests`. Grove proof decode/encode roundtrip byte parity is covered for Rust-generated fixtures. Proof verification API expanded: `VerifySubsetQuery` mirroring Rust `verify_subset_query` (verify_proof_succinctness: false) for non-minimal proofs, `VerifyQueryWithAbsenceProof` mirroring Rust `verify_query_with_absence_proof` (absence_proofs_for_non_existing_searched_keys: true) requiring limit parameter, `VerifySubsetQueryWithAbsenceProof`, `VerifyPathQueryProofWithChainedQueries`, `VerifyQueryGetParentTreeInfo`, plus version-gated wrappers (`*ForVersion`); Rust-backed parity fixtures cover subset/absence verification across versions 4.0.0/4.1.0/5.0.0. Layered proof byte parity was resolved 2026-02-21 (P-202): C++ `Node` struct now stores `feature_type` member populated from `TreeNodeInner`, hash computation checks per-node feature tag matching Rust's per-node `AggregateData` inspection.
| Full | Full | SPV query proofs | `PathQuery`, `QueryItem`, branches | `query.h` | Full PathQuery/SPV parity is complete with comprehensive Rust fixture coverage. Byte-level parity is explicitly asserted for root single-key query path in `rust_grovedb_proof_parity_tests`. Rust fixture coverage includes: combined reference-chain + nested-conditional traversal (`grove_proof_reference_nested_conditional_subquery.bin`), non-conditional `subquery_path + RangeAfter + limit` (`grove_proof_subquery_path_range_after_limit.bin`), `subquery_path + RangeToInclusive + limit` (`grove_proof_subquery_path_range_to_inclusive_limit.bin`), `subquery_path + RangeTo + limit` (`grove_proof_subquery_path_range_to_limit.bin`), `subquery_path + RangeInclusive + limit` (`grove_proof_subquery_path_range_inclusive_limit.bin`), `subquery_path + RangeFrom + limit` (`grove_proof_subquery_path_range_from_limit.bin`), `subquery_path + RangeAfterToInclusive + limit` (`grove_proof_subquery_path_range_after_to_inclusive_limit.bin`), `subquery_path + RangeAfterTo + limit` (`grove_proof_subquery_path_range_after_to_limit.bin`), `subquery_path + Range + limit` (`grove_proof_subquery_path_range_limit.bin`), conditional subquery `RangeAfter + limit + absence` (`grove_proof_conditional_subquery_path_range_after_limit_absent.bin`), reference + conditional subquery + limit (`grove_proof_reference_conditional_subquery_limit.bin`), and `subquery_path + RangeFrom + limit + absence` (`grove_proof_subquery_path_range_from_limit_absent.bin`). Root-level multi-key query parity covered via `grove_proof_root_multi_key.bin`. Layered proof generation mirrors Rust subquery-key extraction for both `Push` and `PushInverted` ops in `CollectProofKeys`. Layered proof byte parity was resolved 2026-02-21 (P-202). All PathQuery v2 query shapes covered with prove+verify integration tests in `grovedb_facade_proof_tests` and Rust-backed parity assertions in `rust_grovedb_proof_parity_tests`.
| Full | Full | Elements encode/decode | `grovedb_element::Element` | `element.h/.cpp` | Full Element variant parity is complete. Typed insert helpers implemented: `InsertItem`, `InsertEmptyTree`, `InsertBigSumTree`, `InsertCountTree`, `InsertProvableCountTree`, `InsertProvableCountSumTree`, `InsertSumItem`, `InsertItemWithSum`, `InsertReference` (all with plain/cost/tx/cost+tx overloads). Decode flows implemented: `ExtractBigSumValueFromElementBytes`, `ExtractCountValueFromElementBytes`, `ExtractCountSumValueFromElementBytes`, `ExtractProvableCountSumValueFromElementBytes`. All variant decoders implemented with contract coverage: `DecodeTreeFromElementBytes` (variant 2), `DecodeSumTreeFromElementBytes` (variant 4), `DecodeBigSumTreeFromElementBytes` (variant 5), `DecodeCountTreeFromElementBytes` (variant 6), `DecodeCountSumTreeFromElementBytes` (variant 7), `DecodeProvableCountTreeFromElementBytes` (variant 8), `DecodeProvableCountSumTreeFromElementBytes` (variant 10). Root-key rewrite propagation preserves `Element::Tree` and `Element::SumTree` flags (Rust-aligned `update_tree_item_preserve_flag` behavior). Rust/C++ byte parity verified for facade insert primitives via `rust_grovedb_facade_parity_tests`. Contract coverage in `element_api_contract_tests` and `grovedb_facade_api_contract_tests`. |
| Full | Full | Reference paths | `ReferencePathType` | `ReferencePathType` | Full reference-path parity is complete. `GroveDb::FollowReference` implements all reference path traversal types with Rust-aligned semantics: empty-qualified-path handling returns `empty reference` error, non-tree parent-layer traversal failures map to `corrupted reference path parent layer not found`, multi-hop mixed path type chain resolution (`UpstreamRootHeight` → `Sibling` → `Absolute` → target), `UpstreamRootHeightWithParentPathAddition` traversal, `UpstreamFromElementHeight` traversal (discards N segments and appends reference path), `CousinReference` traversal (swaps parent key while retaining original key), and `RemovedCousinReference` traversal (swaps parent key with a path, retaining original key). All traversal types covered by comprehensive Rust-backed parity modes (`facade_follow_reference_mixed_path_chain`, `facade_follow_reference_parent_path_addition`, `facade_follow_reference_upstream_element_height`, `facade_follow_reference_cousin`, `facade_follow_reference_removed_cousin`) with contract coverage in `grovedb_facade_api_contract_tests` and passing assertions in `rust_grovedb_facade_parity_tests`.
| Full | Full | Reference chains | Rust reference resolution | `ResolveReferenceChain` | Full reference-chain verification parity is complete. Max-hop limit is supported and enforced. Layered proof verification handles reference chains correctly. Reference-subquery fixtures that omit lower layers match Rust verifier behavior with empty result set (`reference_chain_wrapped_limit`, `reference_hop_limit`, `reference_empty_key_sibling`, `reference_nested_conditional_subquery` in `rust_grovedb_proof_parity_tests`). Hop-limit overflow in verifier-side reference traversal is aligned with Rust semantics (stop resolving branch instead of failing verification), covered by fixture `grove_proof_reference_hop_limit_materialized_chain.bin`. All reference-chain behaviors covered by Rust-backed parity assertions in `rust_grovedb_proof_parity_tests`.
| Full | Full | Subtrees | `Tree` elements, layered Merk | `GroveDb::{IsEmptyTree, DeleteUpTreeWhileEmpty, RootHash, VerifyGroveDb, FindSubtrees, FindSubtreesByKinds, CheckSubtreeExistsInvalidPath, CountSubtrees, GetSubtreeRoot, DeleteSubtree, ClearSubtree}` | Full subtree orchestration parity is complete. Implemented APIs: `IsEmptyTree` (path validation + subtree open + empty/non-empty result with tx state handling), `DeleteUpTreeWhileEmpty` (strict first-level non-empty rejection, upward empty-parent pruning, `stop_path_height` parameter support, tx/cost overloads), `RootHash` (Rust `root_hash` parity: opens root merk, returns cached root hash, tx/non-tx overloads), `VerifyGroveDb` (Rust `verify_grovedb` parity: recursive subtree hash verification checking value_hash+root_hash consistency, tx/non-tx overloads), `FindSubtrees` (Rust `find_subtrees`-style recursive subtree discovery including root-path seed, tx-local visibility, non-tx/tx cost-overload parity), `FindSubtreesByKinds` (filtered subtree discovery by tree kind: 2=Tree, 4=SumTree, 5=BigSumTree, 6=CountTree, 7=CountSumTree, 8=ProvableCountTree, 10=ProvableCountSumTree, plain/cost/tx/cost+tx overloads), `CheckSubtreeExistsInvalidPath` (Rust `check_subtree_exists_invalid_path` parity: empty path accepted, non-tree/missing subtree target rejected, tx-local visibility), `CountSubtrees` (4 overloads: plain/cost/tx/cost+tx for counting without enumeration), `GetSubtreeRoot` (4 overloads for retrieving root element of specific subtree), `DeleteSubtree` (4 overloads with recursive nested subtree deletion), `ClearSubtree` (bulk-delete all keys in subtree with tx/cost overloads). All APIs covered by contract tests in `grovedb_facade_api_contract_tests`. Rust-backed parity modes: `facade_find_subtrees` (nested tree discovery), `facade_check_subtree_exists_invalid_path_tx` (tx-local visibility). |
| Full | Full | Tree kinds | Sum/BigSum/Count/CountSum/ProvableCount/Sum | Merk aggregate metadata | Full tree-kind operation-level parity is complete. Facade root-key rewrite propagation preserves aggregate payload + flags for `CountTree`, `BigSumTree`, `CountSumTree`, `ProvableCountTree`, and `ProvableCountSumTree` during nested subtree propagation (`grovedb_facade_api_contract_tests`). `CountTree` propagation refreshes parent count from child-subtree aggregate (Rust-aligned `update_tree_item_preserve_flag` count rewrite) covered by `grovedb_facade_tests`. Rust-backed persisted parity covers all tree variants via `rust_merk_feature_storage_parity_tests` with mutation fixtures: `merk_feature_sum_mut`, `merk_feature_big_sum_mut`, `merk_feature_big_sum_delete_reinsert`, `merk_feature_big_sum_multi_mut`, `merk_feature_count_mut`, `merk_feature_count_sum_mut`, `merk_feature_count_sum_delete_reinsert`, `merk_feature_prov_count_mut`, `merk_feature_prov_count_sum_mut`, `merk_feature_prov_count_sum_delete_reinsert`, `merk_feature_prov_count_delete_reinsert`. Delete/reinsert fixtures use structural-divergence semantic parity validation (same keys + same aggregate sums) due to AVL balancing differences between Rust batch `apply()` and C++ individual Insert/Delete calls. All feature-variant persisted-format parity tests pass for basic/sum/big_sum/count/count_sum/prov_count/prov_count_sum. |
| Full | Full | Cost accounting | `grovedb_costs`, `OperationCost` | `StorageCost`, `OperationCost` | Storage and Merk cost-shape parity are enforced with Rust fixtures and cost-shape parity tests (`merk_cost_shape_tests`, `rust_storage_cost_shape_parity_tests`). GroveDB facade mutation APIs expose `OperationCost` (`Insert`/`Delete`) with persistence-write cost plumbing and dedicated tests. Rust-backed strict facade mutation parity now passes via `rust_grovedb_facade_cost_writer` + `rust_grovedb_facade_cost_parity_tests` (`GROVEDB_RUN_RUST_PARITY=1`). |
| Full | Full | Versioning | `GroveVersion` | `GroveVersion` + version-gated API wrappers | Version parse/support contracts are covered; version-gated facade coverage is complete with 12 API families each with plain/cost/tx/cost+tx overloads where applicable: `DeleteSubtreeForVersion`, `RootHashForVersion`, `VerifyGroveDbForVersion`, `CheckSubtreeExistsInvalidPathForVersion`, `CountSubtreesForVersion`, `IsEmptyTreeForVersion`, `DeleteIfEmptyTreeForVersion`, `QueryItemValueForVersion`, `QueryKeysOptionalForVersion`, `QuerySumsForVersion`, `QueryRawForVersion`, `ProveQueryForVersion`. All version-gated wrappers enforce unsupported-version rejection and supported-version delegation, covered in `version_api_contract_tests`. Non-latest behavior matrix is covered by `version_matrix_parity_tests` + `version_matrix_report`, including fixture execution across supported versions (`4.0.0`, `4.1.0`, `5.0.0`). `grovedb_facade_api_contract_tests` enforces unsupported-version rejection consistency on the full version-gated API set, including tx-overload ordering vs inactive/committed handles.
| Full | Full | Checkpoints | `checkpoints` | `RocksDbWrapper::CreateCheckpoint`, `GroveDb::{CreateCheckpoint, OpenCheckpoint, DeleteCheckpoint}` | Full GroveDB checkpoint lifecycle parity is complete. Storage checkpoint parity exists with facade wrappers covering create/open/delete checkpoint lifecycle with contract tests. Rust-backed facade tx parity covers checkpoint snapshot isolation (`tx_checkpoint_snapshot_isolation`), independent post-checkpoint write isolation between main DB and checkpoint (`tx_checkpoint_independent_writes`), delete-safety semantics (`tx_checkpoint_delete_safety`), open-safety semantics (`tx_checkpoint_open_safety`), delete short-path hardening (`tx_checkpoint_delete_short_path_safety`), missing-path open hardening (`tx_checkpoint_open_missing_path`), checkpoint reopen/mutate/recheckpoint snapshot isolation (`tx_checkpoint_reopen_mutate_recheckpoint`), chained checkpoint-from-checkpoint reopen/mutate snapshot boundaries (`tx_checkpoint_reopen_mutate_chain`), delete/reopen/recreate edge sequencing across chained checkpoints (`tx_checkpoint_delete_reopen_sequence`), aux-column snapshot isolation across checkpoint/main mutations (`tx_checkpoint_aux_isolation`), checkpoint creation during active transaction (`tx_checkpoint_tx_operations`), checkpoint chain mutation isolation (`tx_checkpoint_chain_mutation_isolation`), checkpoint remains usable after main DB deletion (`tx_checkpoint_reopen_after_main_delete`), and batch operations with checkpoint snapshot isolation (`tx_checkpoint_batch_ops`). C++ checkpoint creation mirrors Rust `create_checkpoint_with_log_size(..., u64::MAX)` semantics (no checkpoint-triggered WAL flush) with storage regression coverage in `tests/storage_tests.cpp`. All checkpoint lifecycle flows covered by Rust-backed parity fixtures in `rust_grovedb_facade_parity_tests`. |
| Full | Full | Replication | `replication` | `replication.h` (`PackNestedBytes`, `UnpackNestedBytes`, `EncodeGlobalChunkId`, `DecodeGlobalChunkId`, `*ForVersion`, `StartStateSyncSession`, `FetchStateSyncChunk`, `ApplyStateSyncChunk`, `IsStateSyncSessionCompleted`, `CommitStateSyncSession`, `IsStateSyncSessionEmpty`) | Full replication API surface is complete. C++ implements Rust-aligned state-sync session lifecycle with version-gated encoding utilities (`kCurrentStateSyncVersion = 1`), malformed-input/roundtrip coverage in `replication_tests`, and comprehensive Rust-backed parity fixtures. APIs implemented: `PackNestedBytes`/`UnpackNestedBytes` (nested byte encoding), `EncodeGlobalChunkId`/`EncodeGlobalChunkIdForVersion` (global chunk ID encoding with version gate), `DecodeGlobalChunkId`/`DecodeGlobalChunkIdForVersion` (global chunk ID decoding with version gate), `IsSupportedStateSyncVersion` (version validation), `StartStateSyncSession` (session initialization with root-hash restorer), `FetchStateSyncChunk` (chunk fetching with in-flight tracking), `ApplyStateSyncChunk` (chunk application with next-ID derivation via ChunkRestorer), `IsStateSyncSessionCompleted` (session status query mirroring Rust `is_sync_completed`), `IsStateSyncSessionEmpty` (session empty status query mirroring Rust `is_empty`), `CommitStateSyncSession` (session finalization mirroring Rust `commit_session`). Rust-backed parity coverage via `rust_replication_writer` + `rust_replication_parity_tests`: byte-equivalent encoded global chunk ID + version gate checks, deterministic session flow fixtures (start/fetch/apply/completion + commit-finalization), zero `subtrees_batch_size` start-session rejection (mirroring Rust `start_snapshot_syncing` validation), nested subtree discovery parity (`AssertReplicationNestedSubtreeDiscoveryParity`), pending chunks count parity (`AssertReplicationPendingChunksParity`), fetch chunk IDs parity (`AssertReplicationFetchChunkIdsParity`), version mismatch apply rejection (`AssertReplicationVersionMismatchApplyParity`), and version rejection parity (`AssertReplicationVersionRejectionParity`). All replication behaviors covered by contract tests in `replication_tests` and Rust-backed assertions in `rust_replication_parity_tests`.
| Divergent | Full | Visualizer / debugger | `grovedbg` support | `GroveDb::StartVisualizer` (`include/grovedb.h`) | Rust exposes `start_visualizer` behind the `grovedbg` feature, while C++ keeps a deterministic unsupported contract through `GroveDb::StartVisualizer(addr, error)` with expiry `2026-06-30` and parity gap reference `visualizer-hook-parity-decision`; active accepted divergence is tracked as `visualizer-grovedbg-scope` in `docs/accepted-divergences.md` and covered by `tests/grovedb_facade_api_contract_tests.cpp`. |
| Full | Full | Storage abstraction | `grovedb_storage` | `RocksDbWrapper`, `MerkStorage` | Full storage-context parity is complete. C++ implements Rust-aligned transactional storage contexts with comprehensive visibility boundary coverage. Transaction-prefixed context implements batch-backed staging (`TransactionPrefixedContext(..., WriteBatch*)`) that stages `Put`/`Delete` into shared batch state with visibility only after `CommitBatch(..., tx)`, matching Rust `StorageContext` staging flow. No-batch writes match Rust behavior by treating `Put`/`Delete` as no-ops while preserving transactional reads. Cross-context multi-batch orchestration mirrors Rust `StorageContext::new_batch` + `commit_batch` semantics via `TransactionPrefixedContext::CommitBatchPart` and `WriteBatch::Append`. Rust-backed parity coverage includes 13 comprehensive modes: `tx_ctx_batch_visibility` (staged write visibility), `tx_ctx_batch_delete_visibility` (staged delete visibility), `tx_ctx_batch_cross_context_visibility` (cross-context staged visibility), `tx_ctx_batch_cross_context_delete_visibility` (cross-context staged delete), `tx_ctx_batch_cross_context_aux_visibility` (cross-context aux visibility), `tx_ctx_batch_cross_context_roots_meta_visibility` (cross-context roots/meta visibility), `tx_ctx_no_batch_noop_multi_context` (no-batch no-op semantics), `tx_ctx_multi_batch_orchestration` (multi-batch merge), `tx_ctx_iterator_staged_writes` (iterator over staged writes), `tx_ctx_delete_prefix_visibility` (staged delete prefix visibility), `tx_ctx_iterator_roots_meta_staged` (iterator over staged roots/meta), `tx_ctx_aux_batch_composition` (aux batch composition), and `tx_ctx_iterator_over_staged_roots_meta` (iterator behavior over staged roots/meta); all exercised by `rust_storage_parity_tests` with Rust writer fixtures in `rust_storage_writer`; validates staged-write/staged-delete visibility boundaries, cross-context visibility isolation, no-batch no-op semantics, multi-batch merge, iterator visibility boundaries, aux/roots/meta batch composition, and post-commit persistence with cross-transaction isolation. |
| Full | Full | Proof verification | `grovedb::verify` | `VerifyPathQueryProof` + proof decode | Full GroveDB verification parity is complete. Layered PathQuery verification exists (incl. reference chains); Rust-fixture parity covers single-key present/absence, root-level query-item range variants (`Range`, `RangeInclusive`, `RangeFull`, `RangeToInclusive`, `RangeTo`, `RangeFrom`, `RangeAfter`, `RangeAfterTo`, `RangeAfterToInclusive`, incl. descending `left_to_right=false` coverage), direct non-root subtree query-shape verification (`path=["root","key","branch"]` + `RangeFull`), wrapped subquery variants, offset contract behavior, and subquery_path `left_to_right` propagation. Chained verification parity includes `VerifyPathQueryProofWithChainedQueries` (+ versioned wrapper) with Rust-backed fixture coverage using `grove_proof_subquery_path.bin` for sequential subset verification under a shared proof/root hash. Absence/subset verification parity includes `VerifySubsetQuery` mirroring Rust `verify_subset_query` (verify_proof_succinctness: false), `VerifyQueryWithAbsenceProof` mirroring Rust `verify_query_with_absence_proof` (absence_proofs_for_non_existing_searched_keys: true) requiring limit parameter, `VerifySubsetQueryWithAbsenceProof`, `VerifyQueryGetParentTreeInfo`, plus version-gated wrappers (`*ForVersion`); Rust-backed parity fixtures cover subset/absence verification across versions 4.0.0/4.1.0/5.0.0 with required-limit contract validation. All verification APIs implemented: `VerifyPathQueryProofWithChainedQueries`, `VerifySubsetQuery`, `VerifyQueryWithAbsenceProof`, `VerifySubsetQueryWithAbsenceProof`, `VerifyQueryGetParentTreeInfo`, and version-gated variants. Rust-backed parity tests pass in `rust_grovedb_proof_parity_tests`. |
| Full | Full | PathQuery v2 flag | `add_parent_tree_on_subquery` | `PathQuery::add_parent_tree_on_subquery` | Full PathQuery v2 parity is complete. Rust-fixture parity covers subquery child traversal, parent inclusion shape (`false/true`, including optional parent value), conditional-branch terminal override, default `subquery_path`, conditional-branch `subquery_path`, nested conditional chains, `add_parent_tree_on_subquery=true` with wrapped `subquery_path`, bounded `SizedQuery(limit=1)` traversal shape (ascending + descending fixture coverage), wrapped subquery non-full inner range filtering (`RangeAfter`, `RangeToInclusive`, `RangeTo`, `RangeFrom`). Comprehensive query-shape fixtures implemented: non-conditional `subquery_path + RangeFrom + limit` (`grove_proof_subquery_path_range_from_limit.bin`), `subquery_path + RangeInclusive + limit` (`grove_proof_subquery_path_range_inclusive_limit.bin`), `subquery_path + RangeAfterToInclusive + limit` (`grove_proof_subquery_path_range_after_to_inclusive_limit.bin`), `subquery_path + RangeAfterTo + limit` (`grove_proof_subquery_path_range_after_to_limit.bin`), `subquery_path + Range + limit` (`grove_proof_subquery_path_range_limit.bin`), `subquery_path + RangeFrom + limit + absence` (`grove_proof_subquery_path_range_from_limit_absent.bin`); conditional subquery fixtures: `RangeAfter + limit + absence` (`grove_proof_conditional_subquery_path_range_after_limit_absent.bin`), reference + conditional subquery + limit (`grove_proof_reference_conditional_subquery_limit.bin`); conditional non-full + limit matrix (`Range`, `RangeInclusive`, `RangeAfter`, `RangeAfterTo`, `RangeAfterToInclusive`, `RangeToInclusive`, `RangeTo`, `RangeFrom`). All fixtures covered by C++ verifier assertions in `rust_grovedb_proof_parity_tests`. Layered byte parity was resolved 2026-02-21 (P-202). |

## Notes

- C++ rewrite parity status for Rust GroveDB v4.0.0 is tracked by this matrix and supporting gate docs/scripts.
- Current test totals can change over time; use `ctest --output-on-failure` and `tools/run_parity_ctest.sh all` for live status.
- CI parity workflow is defined in `.github/workflows/ci.yml` (semantic/byte/cost/contract gates + policy checks + artifacts).
